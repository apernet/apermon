#ifndef APERMON_CONTEXT_H
#define APERMON_CONTEXT_H
#include <search.h>

typedef struct _apermon_context {
    // todo hash table struct to store pps/bps from/to ip - or anything else that might be useful
} apermon_context;

#endif // APERMON_CONTEXT_H